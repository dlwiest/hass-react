"use strict";(globalThis.webpackChunkdocs=globalThis.webpackChunkdocs||[]).push([[931],{6137:(e,n,t)=>{t.r(n),t.d(n,{assets:()=>c,contentTitle:()=>i,default:()=>h,frontMatter:()=>o,metadata:()=>s,toc:()=>d});const s=JSON.parse('{"id":"advanced/building-a-gateway","title":"Building a Gateway","description":"External Transports covers the contract hass-react expects. This page builds the other half: the gateway server that owns your Home Assistant credential, plus the browser transport that talks to it. The complete runnable version lives in examples/transport-gateway.","source":"@site/docs/advanced/building-a-gateway.md","sourceDirName":"advanced","slug":"/advanced/building-a-gateway","permalink":"/docs/advanced/building-a-gateway","draft":false,"unlisted":false,"editUrl":"https://github.com/dlwiest/hass-react/tree/master/docs/docs/advanced/building-a-gateway.md","tags":[],"version":"current","sidebarPosition":4,"frontMatter":{"sidebar_position":4},"sidebar":"tutorialSidebar","previous":{"title":"External Transports","permalink":"/docs/advanced/external-transports"},"next":{"title":"Error Handling & Connection Status","permalink":"/docs/error-handling"}}');var r=t(4848),a=t(8453);const o={sidebar_position:4},i="Building a Gateway",c={},d=[{value:"The server",id:"the-server",level:2},{value:"The client transport",id:"the-client-transport",level:2},{value:"Wiring it up",id:"wiring-it-up",level:2},{value:"What failure looks like",id:"what-failure-looks-like",level:2},{value:"Hardening beyond the LAN",id:"hardening-beyond-the-lan",level:2}];function l(e){const n={a:"a",code:"code",h1:"h1",h2:"h2",header:"header",li:"li",p:"p",pre:"pre",strong:"strong",ul:"ul",...(0,a.R)(),...e.components};return(0,r.jsxs)(r.Fragment,{children:[(0,r.jsx)(n.header,{children:(0,r.jsx)(n.h1,{id:"building-a-gateway",children:"Building a Gateway"})}),"\n",(0,r.jsxs)(n.p,{children:[(0,r.jsx)(n.a,{href:"/docs/advanced/external-transports",children:"External Transports"})," covers the contract hass-react expects. This page builds the other half: the gateway server that owns your Home Assistant credential, plus the browser transport that talks to it. The complete runnable version lives in ",(0,r.jsx)(n.a,{href:"https://github.com/dlwiest/hass-react/tree/master/examples/transport-gateway",children:(0,r.jsx)(n.code,{children:"examples/transport-gateway"})}),"."]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-text",children:"Home Assistant  <-- WebSocket, long-lived token --\x3e  gateway (Node)\ngateway  <-- SSE events + HTTP commands --\x3e  browser running hass-react\n"})}),"\n",(0,r.jsxs)(n.p,{children:["The browser never sees the token. It sends allowlisted commands to an HTTP endpoint and receives ",(0,r.jsx)(n.code,{children:"state_changed"})," events over one Server-Sent Events stream. hass-react treats the pair as a connection and keeps all of its usual behavior: entity subscriptions, service calls, and reconnection."]}),"\n",(0,r.jsx)(n.h2,{id:"the-server",children:"The server"}),"\n",(0,r.jsxs)(n.p,{children:["Node 22 or newer, one dependency (",(0,r.jsx)(n.code,{children:"home-assistant-js-websocket"}),", the same client Home Assistant's frontend uses; Node 22 provides the WebSocket implementation it needs)."]}),"\n",(0,r.jsx)(n.p,{children:"Start with the part that makes a gateway worth having, the authorization boundary:"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-js",children:"const ALLOWED_COMMANDS = new Set(['get_states', 'auth/current_user'])\n\n// domain -> allowed services. Extend deliberately, entry by entry.\nconst ALLOWED_SERVICES = {\n  light: new Set(['turn_on', 'turn_off', 'toggle']),\n  switch: new Set(['turn_on', 'turn_off', 'toggle']),\n}\n\n// domain -> entities the browser may target. null = any entity in the domain.\n// Scope this to the entities your dashboard actually shows.\nconst ALLOWED_ENTITIES = {\n  light: null,\n  switch: null,\n}\n\nfunction targetedEntities(message) {\n  // Home Assistant accepts the target in either field, as one id or a list.\n  const raw = message.service_data?.entity_id ?? message.target?.entity_id\n  if (raw === undefined) return []\n  return Array.isArray(raw) ? raw : [raw]\n}\n\nfunction authorize(message) {\n  if (ALLOWED_COMMANDS.has(message.type)) return true\n  if (message.type !== 'call_service') return false\n\n  if (!(ALLOWED_SERVICES[message.domain]?.has(message.service) ?? false)) {\n    return false\n  }\n\n  const allowedEntities = ALLOWED_ENTITIES[message.domain]\n  // A domain allowlisted without an entity decision fails closed.\n  if (allowedEntities === undefined) return false\n  if (allowedEntities === null) return true\n  const targets = targetedEntities(message)\n  return targets.length > 0 && targets.every((id) => allowedEntities.has(id))\n}\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Everything the browser may do is enumerated: commands, services per domain, and optionally entities per domain. A request that doesn't match is rejected before it touches Home Assistant, so a compromised or curious client can't call ",(0,r.jsx)(n.code,{children:"homeassistant.stop"})," just because the gateway is reachable. Setting an entity set instead of ",(0,r.jsx)(n.code,{children:"null"})," narrows control to exactly the devices your dashboard shows."]}),"\n",(0,r.jsxs)(n.p,{children:["The upstream side is a normal ",(0,r.jsx)(n.code,{children:"home-assistant-js-websocket"})," connection with a long-lived token. One ",(0,r.jsx)(n.code,{children:"state_changed"})," subscription fans out to every connected browser:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-js",children:"import {createConnection, createLongLivedTokenAuth} from 'home-assistant-js-websocket'\n\nconst auth = createLongLivedTokenAuth(process.env.HA_URL, process.env.HA_TOKEN)\nconst connection = await createConnection({auth})\n\nconst sseClients = new Set()\n\nawait connection.subscribeEvents((event) => {\n  const payload = `data: ${JSON.stringify(event)}\\n\\n`\n  for (const res of sseClients) res.write(payload)\n}, 'state_changed')\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Upstream reconnection is free: ",(0,r.jsx)(n.code,{children:"createConnection"})," retries on its own when Home Assistant restarts or drops."]}),"\n",(0,r.jsxs)(n.p,{children:["The browser-facing half is two routes on a plain ",(0,r.jsx)(n.code,{children:"http"})," server. ",(0,r.jsx)(n.code,{children:"GET /api/events"})," registers an SSE client; ",(0,r.jsx)(n.code,{children:"POST /api/command"})," parses one JSON message, authorizes it, and forwards it:"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-js",children:"if (req.method === 'POST' && req.url === '/api/command') {\n  const message = JSON.parse(body) // bound the body size before this\n\n  if (!authorize(message)) {\n    res.writeHead(403).end(JSON.stringify({error: `not allowed: ${message.type}`}))\n    return\n  }\n\n  try {\n    const result = await connection.sendMessagePromise(message)\n    res.writeHead(200, {'content-type': 'application/json'})\n    res.end(JSON.stringify(result ?? null))\n  } catch (error) {\n    const ref = crypto.randomUUID()\n    console.error(`command failed [${ref}]`, error)\n    res.writeHead(502).end(JSON.stringify({error: 'upstream command failed', ref}))\n  }\n}\n"})}),"\n",(0,r.jsx)(n.p,{children:"Upstream error details stay in the gateway log. The browser gets a reference id, not Home Assistant internals."}),"\n",(0,r.jsx)(n.h2,{id:"the-client-transport",children:"The client transport"}),"\n",(0,r.jsxs)(n.p,{children:["The browser half implements ",(0,r.jsx)(n.code,{children:"HATransport"})," over ",(0,r.jsx)(n.code,{children:"fetch"})," and ",(0,r.jsx)(n.code,{children:"EventSource"}),":"]}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-js",children:"export function createGatewayTransport(baseUrl) {\n  // connection -> its EventSource, so disconnect() closes the right session.\n  const sessions = new Map()\n\n  return {\n    connect(handlers) {\n      return new Promise((resolve, reject) => {\n        const events = new EventSource(`${baseUrl}/api/events`)\n        const subscribers = new Map()\n        let settled = false\n        let notifiedDisconnect = false\n\n        const connection = {\n          async sendMessagePromise(message) {\n            const response = await fetch(`${baseUrl}/api/command`, {\n              method: 'POST',\n              headers: {'content-type': 'application/json'},\n              body: JSON.stringify(message),\n            })\n            if (!response.ok) throw new Error(`gateway returned ${response.status}`)\n            return response.json()\n          },\n\n          async subscribeEvents(callback, eventType) {\n            const callbacks = subscribers.get(eventType) ?? new Set()\n            callbacks.add(callback)\n            subscribers.set(eventType, callbacks)\n            return () => {\n              callbacks.delete(callback)\n            }\n          },\n        }\n\n        events.onopen = () => {\n          settled = true\n          sessions.set(connection, events)\n          resolve(connection)\n        }\n\n        events.onmessage = ({data}) => {\n          const event = JSON.parse(data)\n          subscribers.get(event.event_type)?.forEach((cb) => cb(event))\n          subscribers.get(undefined)?.forEach((cb) => cb(event))\n        }\n\n        events.onerror = () => {\n          if (!settled) {\n            settled = true\n            events.close()\n            reject(new Error('gateway event stream unavailable'))\n          } else if (!notifiedDisconnect) {\n            notifiedDisconnect = true\n            events.close()\n            sessions.delete(connection)\n            handlers.onDisconnected()\n          }\n        }\n      })\n    },\n\n    disconnect(connection) {\n      sessions.get(connection)?.close()\n      sessions.delete(connection)\n    },\n  }\n}\n"})}),"\n",(0,r.jsxs)(n.p,{children:["One design rule matters here: ",(0,r.jsxs)(n.strong,{children:["every ",(0,r.jsx)(n.code,{children:"connect()"})," call builds an independent session."]})," The EventSource, the subscriber registry, and the settled flags all live inside the call. HAProvider can legitimately run overlapping connect attempts (React StrictMode double-mounts in development, and fast reconnect cycles in production), and it disconnects the attempts it abandons. If sessions share state, a stale attempt's ",(0,r.jsx)(n.code,{children:"disconnect()"})," tears down the live one and the provider hangs in ",(0,r.jsx)(n.code,{children:"connecting"}),". We know because our first draft did exactly that."]}),"\n",(0,r.jsxs)(n.p,{children:["The transport also reports a lost stream once and stops. ",(0,r.jsx)(n.code,{children:"EventSource"})," would happily retry on its own, but HAProvider owns reconnection policy; let it call ",(0,r.jsx)(n.code,{children:"connect()"})," again with its own backoff."]}),"\n",(0,r.jsx)(n.h2,{id:"wiring-it-up",children:"Wiring it up"}),"\n",(0,r.jsx)(n.pre,{children:(0,r.jsx)(n.code,{className:"language-jsx",children:"import { HAProvider } from 'hass-react'\nimport { createGatewayTransport } from './gateway-transport'\n\nconst transport = createGatewayTransport('http://dashboard.local:8131')\n\nfunction App() {\n  return (\n    <HAProvider url=\"http://dashboard.local:8131\" transport={transport}>\n      <Dashboard />\n    </HAProvider>\n  )\n}\n"})}),"\n",(0,r.jsxs)(n.p,{children:["Create the transport once, outside the component (or memoize it). ",(0,r.jsx)(n.code,{children:"url"})," only resolves relative media URLs in this mode."]}),"\n",(0,r.jsx)(n.h2,{id:"what-failure-looks-like",children:"What failure looks like"}),"\n",(0,r.jsx)(n.p,{children:"Verified against the example implementation:"}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.strong,{children:"Gateway dies:"})," the transport reports the loss, hass-react shows ",(0,r.jsx)(n.code,{children:"disconnected"}),", then retries ",(0,r.jsx)(n.code,{children:"connect()"})," on its usual backoff (1s, 2s, 4s...). When the gateway returns, the next attempt connects and entity state re-hydrates."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.strong,{children:"Home Assistant dies:"})," the gateway's upstream client reconnects on its own. Browsers stay connected to the gateway throughout; commands during the outage fail with 502 and a log reference."]}),"\n",(0,r.jsxs)(n.li,{children:[(0,r.jsx)(n.strong,{children:"Blocked command:"})," the gateway returns 403, which surfaces in hass-react as a normal failed service call."]}),"\n"]}),"\n",(0,r.jsx)(n.h2,{id:"hardening-beyond-the-lan",children:"Hardening beyond the LAN"}),"\n",(0,r.jsx)(n.p,{children:"The example assumes a trusted network, which fits the wall-dashboard case. Before exposing a gateway any wider:"}),"\n",(0,r.jsxs)(n.ul,{children:["\n",(0,r.jsx)(n.li,{children:"Put a session in front of both routes (cookie or reverse-proxy auth). Token possession is the whole game."}),"\n",(0,r.jsxs)(n.li,{children:["Set ",(0,r.jsx)(n.code,{children:"ALLOWED_ORIGIN"})," to your dashboard's exact origin instead of ",(0,r.jsx)(n.code,{children:"*"}),"."]}),"\n",(0,r.jsxs)(n.li,{children:["Add rate limiting on ",(0,r.jsx)(n.code,{children:"/api/command"}),"."]}),"\n",(0,r.jsxs)(n.li,{children:["If the dashboard shows cameras, proxy ",(0,r.jsx)(n.code,{children:"/api/camera_proxy"})," and ",(0,r.jsx)(n.code,{children:"/api/hls/*"})," too; see ",(0,r.jsx)(n.a,{href:"/docs/advanced/external-transports#camera-media",children:"Camera Media"}),"."]}),"\n"]}),"\n",(0,r.jsxs)(n.p,{children:["The ",(0,r.jsx)(n.a,{href:"/docs/advanced/external-transports#security-boundary",children:"Security Boundary"})," section covers the full checklist."]})]})}function h(e={}){const{wrapper:n}={...(0,a.R)(),...e.components};return n?(0,r.jsx)(n,{...e,children:(0,r.jsx)(l,{...e})}):l(e)}},8453:(e,n,t)=>{t.d(n,{R:()=>o,x:()=>i});var s=t(6540);const r={},a=s.createContext(r);function o(e){const n=s.useContext(a);return s.useMemo(function(){return"function"==typeof e?e(n):{...n,...e}},[n,e])}function i(e){let n;return n=e.disableParentContext?"function"==typeof e.components?e.components(r):e.components||r:o(e.components),s.createElement(a.Provider,{value:n},e.children)}}}]);